apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n-web-deployment
  labels:
    app: n8n-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: n8n-web
  template:
    metadata:
      labels:
        app: n8n-web
    spec:
      containers:
        - name: execution-web
          image: ikrambagban/n8n-web:af14d511413f170bb7e85b098d749b04baf97ff8
          ports:
            - containerPort: 3000
          envFrom:
          - secretRef:
              name: n8n-web-secret